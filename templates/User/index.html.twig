{% extends 'base.html.twig' %}
{% block title %}
    Vos informations
{% endblock %}
{% block body %}
    <h1 class="text-center">Votre Compte</h1>
    <section class="container my-3">
        <div class="row">
            <div class="col">
                {{ form(form) }}
            </div>
        </div>
        <a href="{{path('user.update', {id: app.user.id})}}"  class="btn btn-warning" role="button">Modifer Vos informations</a>
        <a href="{{path('user.updatePassword', {id: app.user.id})}}"  class="btn btn-info" role="button">Modifer votre Mot de passe</a>
    </section>
{% endblock %}